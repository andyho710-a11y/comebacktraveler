---
import BlogLayout from '../layouts/BlogLayout.astro';
import SocialLinks from '../components/SocialLinks.astro';
import SiteNav from '../components/SiteNav.astro';
import { esimArticles, travelArticles, lifeArticles } from '../data/articles.js';
---

<BlogLayout
  title="重來的旅人｜2026 出國上網 eSIM 比較、旅遊攻略與省錢指南"
  description="提供 2026 最實用的日本、韓國、越南旅遊攻略。深度比較各大品牌 eSIM（JOYTEL、DJB、Klook），並分享高鐵與國旅省錢秘訣。把踩過的坑，變成你的攻略。"
  keywords="eSIM推薦, 2026出國上網, 日本eSIM推薦, 韓國eSIM比較, Klook優惠碼, 高鐵省錢, 重來的旅人"
  ogImage="/esim-cover.jpg"
>
  <link slot="head" rel="preconnect" href="https://fonts.googleapis.com" />
  <link slot="head" href="https://fonts.googleapis.com/css2?family=Noto+Serif+TC:wght@400;600;900&family=Noto+Sans+TC:wght@400;500&display=swap" rel="stylesheet" />

  <div class="root">

    <!-- Header -->
    <SiteNav />

    <!-- Hero -->
    <div class="hero">
      <div class="category-tag">關於這個網站</div>
      <h1>前半生跌了很多跤，<br>現在把坑整理給你</h1>
      <p class="subtitle">
        我在越南工廠當過副總、養蝦失敗、跑過 Uber、父親走了之後才真正開始重新出發。<br>
        這個網站，是一個 40 幾歲台灣男人寫給自己、也寫給你的筆記。
      </p>
      <div class="meta">
        <div class="author-dot">旅</div>
        <div>
          <div class="meta-author">重來的旅人</div>
          <div>越南生活 10 年 · 自媒體行銷 · 重新出發</div>
        </div>
      </div>
    </div>

    <!-- Author Story -->
    <div class="article">
      <div class="story-box">
        <strong>我不是什麼旅遊達人，也沒有光鮮亮麗的履歷。</strong><br><br>
        我是家裡文具工廠的第二代。國中叛逆、KTV 鬧事住院，被送去加拿大九個月帶回來。
        後來去越南幫父親管廠，擔任副總一待就是十年。
        養蝦失敗、環保藥劑事業失敗，靠父親收拾殘局。
        2018 年回台灣，工廠被拍賣，跑起了 Uber。
        外婆走了，父親肺癌末期也走了。那是人生最深的失落。<br><br>
        直到遇見主耶穌，重新找回了方向。
        這個網站，是我把這些年踩過的坑，整理成對你有用的東西。
        <br><br>
        <a href="/story" style="color:var(--accent);font-weight:600;font-size:15px;">→ 看完整的故事</a>
      </div>

    </div><!-- /article (story box) -->

    <!-- Intro content: 提升首頁原創內容與 E-E-A-T -->
    <div class="article intro-content">
      <h2>這個網站在幫你解決什麼</h2>
      <p>
        我寫的東西，都繞著一個主題：<strong>第一次出國、或久久才出一次國的人，怎麼少踩一點坑。</strong>
        我在越南住了十年，從工廠副總到回台灣跑 Uber，這些年自己訂票、自己找路、自己被海關和路邊計程車坑過，
        才慢慢摸出哪些錢該省、哪些錢省不得。這個網站，就是把那些教訓寫成你能直接拿去用的筆記。
      </p>
      <p>目前內容分成三塊，你可以從上方選單或下面的卡片進去：</p>
      <ul class="intro-list">
        <li>
          <strong>出國上網 eSIM</strong>：原生線路和漫遊線路差在哪、哪家有中文客服、安裝失敗怎麼救、
          甚至「封包送中」資安爭議到底在吵什麼，我都用白話拆給你看。完全不業配，只比規格和實際體驗。
        </li>
        <li>
          <strong>日韓越旅遊攻略</strong>：不是觀光局那種「十大必去」，而是哪個時段去人最少、
          哪個景點現場排隊要等多久、哪些票提前訂才划算。越南的部分尤其是我的主場——機場、叫車、親子、行李，
          都是住了十年累積下來的真實經驗。
        </li>
        <li>
          <strong>旅遊省錢情報</strong>：高鐵怎麼買最便宜、各家銀行 Klook 刷卡折扣、信用卡海外回饋怎麼疊。
          跑過 Uber 的人，對每一塊錢都特別敏感。
        </li>
      </ul>
      <p class="intro-note">
        說明一下：本站部分連結是聯盟行銷連結，你透過它們購買，我會拿到一點分潤，但<strong>不會影響你的價格</strong>。
        我只放自己用過、或評估後真心覺得對你有幫助的東西。更完整的揭露寫在
        <a href="/privacy">隱私權政策</a>，想多認識我可以看
        <a href="/about">關於我</a>。
      </p>
    </div>

    <!-- Life notes -->
    <div class="article life-section">
      <h2>人生筆記</h2>
      <p class="section-desc">旅遊與省錢之外，偶爾也寫一點中年重新出發的體悟與思考。</p>
      <div class="card-grid life-grid">
        {lifeArticles.map((a) => (
          <a href={a.href} class="article-card">
            <div class="article-card-tag">{a.tag}</div>
            <div class="article-card-title">{a.title}</div>
            <p class="article-card-desc">{a.desc}</p>
            <div class="article-card-link">{a.label}</div>
          </a>
        ))}
      </div>
    </div>

    <!-- Two-column wide section -->
    <div class="wide-wrap">

      <div class="section-col">
        <h2>出國上網攻略</h2>
        <p class="section-desc">出門在外沒有網路，那種慌是真的。以下 eSIM 選購指南依各家官方規格、線路類型與客服管道彙整，不業配。</p>
        <a href="/esim-quiz" class="quiz-cta">🧭 30 秒測出你該用哪種上網方案 →</a>
        <div class="card-grid">
          {esimArticles.map((a) => (
            <a href={a.href} class="article-card">
              <div class="article-card-tag">{a.tag}</div>
              <div class="article-card-title">{a.title}</div>
              <p class="article-card-desc">{a.desc}</p>
              <div class="article-card-link">{a.label}</div>
            </a>
          ))}
        </div>
      </div>

      <div class="section-col">
        <h2>旅行省錢攻略</h2>
        <p class="section-desc">票怎麼買最便宜、行程怎麼搭最划算、哪個 Klook 活動值得提前訂——跑過 Uber 的人，對每一塊錢都特別敏感。</p>
        <div class="card-grid">
          {travelArticles.map((a) => (
            <a href={a.href} class="article-card">
              <div class="article-card-tag">{a.tag}</div>
              <div class="article-card-title">{a.title}</div>
              <p class="article-card-desc">{a.desc}</p>
              <div class="article-card-link">{a.label}</div>
            </a>
          ))}
        </div>
      </div>

    </div><!-- /wide-wrap -->

    <div class="article"><!-- resume narrow -->

      <!-- Klook 省錢情報（明細移至獨立頁，首頁僅保留導引）-->
      <h2>旅遊省錢情報</h2>
      <p>
        出國訂門票、交通和行程前，記得先看一下手上的信用卡有沒有折扣。
        各大銀行的 Klook 刷卡優惠碼我整理在這頁，會不定期更新：
        <a href="/klook-deals" style="color:var(--accent);font-weight:600;">Klook 銀行優惠碼整理 →</a>
      </p>

      <!-- Social Links -->
      <SocialLinks
        heading="追蹤我｜Follow Me"
        subheading="IG、FB、YouTube 上會分享更多旅遊踩坑筆記與省錢情報"
      />

    </div>

    <!-- Footer -->
    <footer class="site-footer">
      <strong>重來的旅人</strong> · 把踩過的坑，變成你的攻略<br>
      <div class="footer-nav">
        <a href="/esim">eSIM 比較</a>
        <a href="/esim/japan">日本 eSIM</a>
        <a href="/esim/korea">韓國 eSIM</a>
        <a href="/esim/china">中國 eSIM</a>
        <a href="/esim/china-routing-2026">eSIM 封包送中爭議</a>
        <a href="/japan-guide">日本攻略</a>
        <a href="/korea-guide">韓國攻略</a>
        <a href="/vietnam-guide">越南攻略</a>
        <a href="/esim/saigon-airport-sim">胡志明機場 SIM</a>
        <a href="/vietnam-jcb-lounge">越南貴賓室</a>
        <a href="/kids-travel-guide">親子旅遊</a>
        <a href="/thsr-guide">高鐵省錢</a>
        <a href="/about">關於</a>
        <a href="/story">作者故事</a>
        <a href="/contact">聯絡我</a>
        <a href="/klook-deals">Klook 優惠碼</a>
        <a href="/life-restart-naval">人生筆記</a>
        <a href="/services">站長服務</a>
        <a href="/privacy">隱私權政策</a>
      </div>
      <span style="display:block;margin-top:10px;">自媒體行銷 · 越南生活 10 年 · 現在重新出發</span>
    </footer>

  </div>
</BlogLayout>

<script>
  var form = document.getElementById('subscribe-form');
  var success = document.getElementById('subscribe-success');
  if (form && success) {
    form.addEventListener('submit', async function(e) {
      e.preventDefault();
      var data = new FormData(form as HTMLFormElement);
      await fetch('/', { method: 'POST', body: data });
      (form as HTMLElement).hidden = true;
      (success as HTMLElement).hidden = false;
    });
  }
</script>

<style>
  .root {
    --ink: #1a1208;
    --ink-light: #4a3f2f;
    --paper: #faf8f3;
    --paper-warm: #f2ede0;
    --accent: #c0392b;
    --gold: #b8860b;
    --border: #d4c9b0;
    --serif: 'Noto Serif TC', serif;
    --sans: 'Noto Sans TC', sans-serif;
    background: var(--paper);
    color: var(--ink);
    font-family: var(--sans);
    line-height: 1.8;
    font-size: 17px;
    min-height: 100vh;
  }

  /* Header */
  .site-header {
    border-bottom: 2px solid var(--ink);
    padding: 18px 0;
    text-align: center;
    background: var(--paper);
    position: sticky;
    top: 0;
    z-index: 100;
  }
  .site-name {
    font-family: var(--serif);
    font-weight: 900;
    font-size: 20px;
    letter-spacing: 0.12em;
    color: var(--ink);
    text-decoration: none;
  }
  .site-name span { color: var(--accent); }

  /* Hero */
  .hero {
    max-width: 740px;
    margin: 60px auto 0;
    padding: 0 24px;
  }
  .category-tag {
    font-size: 12px;
    font-weight: 500;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--accent);
    border-left: 3px solid var(--accent);
    padding-left: 10px;
    margin-bottom: 20px;
  }
  h1 {
    font-family: var(--serif);
    font-weight: 900;
    font-size: clamp(28px, 5vw, 44px);
    line-height: 1.35;
    margin-bottom: 20px;
  }
  .subtitle {
    font-size: 17px;
    color: var(--ink-light);
    margin-bottom: 32px;
    line-height: 1.75;
  }
  .meta {
    display: flex;
    align-items: center;
    gap: 20px;
    padding: 16px 0;
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
    margin-bottom: 48px;
    font-size: 13px;
    color: var(--ink-light);
  }
  .author-dot {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: var(--ink);
    color: var(--paper);
    font-family: var(--serif);
    font-weight: 900;
    font-size: 15px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }
  .meta-author { font-weight: 500; font-size: 14px; color: var(--ink); }

  /* Article body */
  .article {
    max-width: 740px;
    margin: 0 auto;
    padding: 0 24px 80px;
  }
  .article p { margin-bottom: 1.4em; }

  h2 {
    font-family: var(--serif);
    font-weight: 900;
    font-size: 26px;
    margin: 52px 0 16px;
    position: relative;
    padding-left: 16px;
  }
  h2::before {
    content: '';
    position: absolute;
    left: 0;
    top: 4px;
    width: 4px;
    height: 80%;
    background: var(--accent);
    border-radius: 2px;
  }

  /* Story box */
  .story-box {
    background: var(--paper-warm);
    border-left: 4px solid var(--accent);
    padding: 24px 28px;
    margin: 0 0 40px;
    border-radius: 0 6px 6px 0;
    font-family: var(--serif);
    font-size: 16px;
    color: var(--ink-light);
    line-height: 1.9;
  }
  .story-box strong { color: var(--ink); font-weight: 600; }

  /* Intro content block */
  .intro-content { padding-bottom: 0; }
  .intro-content h2 { margin-top: 8px; }
  .intro-content p { font-size: 16px; color: var(--ink-light); }
  .intro-content strong { color: var(--ink); font-weight: 600; }
  .intro-list { margin: 0 0 1.4em; padding-left: 0; list-style: none; }
  .intro-list li {
    font-size: 15.5px;
    color: var(--ink-light);
    line-height: 1.8;
    padding: 14px 0 14px 18px;
    border-left: 3px solid var(--border);
    margin-bottom: 12px;
  }
  .intro-list li strong { color: var(--ink); }
  .intro-note {
    font-size: 14px;
    color: var(--ink-light);
    background: var(--paper-warm);
    border-radius: 6px;
    padding: 16px 20px;
  }
  .intro-note a { color: var(--accent); }

  /* Life notes section */
  .life-section { padding-top: 0; padding-bottom: 24px; }
  .life-section h2 { margin-top: 8px; }
  .life-grid { grid-template-columns: 1fr; max-width: 620px; }

  /* Wide two-column layout */
  .wide-wrap {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 24px;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0 56px;
    align-items: start;
  }
  .section-col { padding-bottom: 48px; }
  .section-col h2 { margin-top: 52px; }
  .section-desc { margin-bottom: 0; }

  .quiz-cta {
    display: inline-block;
    margin: 16px 0 0;
    padding: 12px 18px;
    border: 1.5px solid var(--accent);
    border-radius: 8px;
    background: #fef9f5;
    color: var(--accent);
    font-weight: 600;
    font-size: 14.5px;
    text-decoration: none;
    transition: background 0.15s;
  }
  .quiz-cta:hover { background: #fbeee6; }

  /* Article cards */
  .card-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 14px;
    margin: 20px 0 0;
  }
  .article-card {
    display: flex;
    flex-direction: column;
    border: 2px solid var(--border);
    border-radius: 8px;
    padding: 22px 20px;
    text-decoration: none;
    color: var(--ink);
    transition: border-color 0.15s, box-shadow 0.15s;
  }
  .article-card:hover {
    border-color: var(--accent);
    box-shadow: 0 4px 16px rgba(192,57,43,0.08);
  }
  .article-card-tag {
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--accent);
    margin-bottom: 10px;
  }
  .article-card-title {
    font-family: var(--serif);
    font-weight: 900;
    font-size: 16px;
    line-height: 1.45;
    margin-bottom: 10px;
    color: var(--ink);
  }
  .article-card-desc {
    font-size: 13.5px;
    color: var(--ink-light);
    line-height: 1.7;
    flex: 1;
    margin-bottom: 16px !important;
  }
  .article-card-link {
    font-size: 13px;
    font-weight: 500;
    color: var(--accent);
    margin-top: auto;
  }

  /* Tip box */
  .tip-box {
    border: 1.5px solid var(--border);
    border-radius: 6px;
    padding: 20px 24px;
    margin: 32px 0 0;
  }
  .tip-title {
    font-weight: 500;
    font-size: 12px;
    letter-spacing: 0.12em;
    color: var(--gold);
    text-transform: uppercase;
    margin-bottom: 8px;
  }

  /* Subscribe box */
  .subscribe-box {
    margin: 56px 0 0;
    border: 2px solid var(--ink);
    border-radius: 8px;
    overflow: hidden;
  }
  .subscribe-inner {
    background: var(--ink);
    padding: 36px 40px;
    text-align: center;
    color: var(--paper);
  }
  .subscribe-icon { font-size: 28px; margin-bottom: 12px; }
  .subscribe-title {
    font-family: var(--serif);
    font-weight: 900;
    font-size: 22px;
    color: var(--paper);
    margin-bottom: 10px;
  }
  .subscribe-desc { font-size: 15px; color: #d4c9b0; margin-bottom: 24px; line-height: 1.6; }
  .subscribe-form { max-width: 440px; margin: 0 auto; }
  .subscribe-row { display: flex; gap: 10px; }
  .subscribe-input {
    flex: 1;
    font-family: var(--sans);
    font-size: 15px;
    padding: 12px 16px;
    border: 1.5px solid #4a3f2f;
    border-radius: 4px;
    background: #2c1f0f;
    color: var(--paper);
    outline: none;
    transition: border-color 0.15s;
  }
  .subscribe-input:focus { border-color: var(--accent); }
  .subscribe-input::placeholder { color: #6b5a46; }
  .subscribe-btn {
    font-family: var(--serif);
    font-weight: 700;
    font-size: 15px;
    padding: 12px 24px;
    background: var(--accent);
    color: #fff;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    white-space: nowrap;
    transition: background 0.15s;
  }
  .subscribe-btn:hover { background: #a93226; }
  .subscribe-note { font-size: 12px; color: #6b5a46; margin-top: 12px; margin-bottom: 0; }
  .subscribe-success { padding: 16px; background: #1c5e15; border-radius: 4px; margin-top: 16px; }
  .subscribe-success p { margin: 0; color: #d5e8d4; font-size: 15px; }

  /* Footer */
  .site-footer {
    border-top: 2px solid var(--ink);
    padding: 40px 24px;
    text-align: center;
    font-size: 13px;
    color: var(--ink-light);
  }
  .site-footer strong { color: var(--ink); }
  .footer-nav {
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    gap: 6px 20px;
    margin-top: 14px;
  }
  .footer-nav a {
    color: var(--accent);
    text-decoration: none;
    font-size: 13px;
  }
  .footer-nav a:hover { text-decoration: underline; }

  /* Responsive */
  @media (max-width: 900px) {
    .wide-wrap {
      grid-template-columns: 1fr;
      gap: 0;
    }
    .card-grid { grid-template-columns: repeat(2, 1fr); }
  }
  @media (max-width: 700px) {
    .card-grid { grid-template-columns: 1fr; }
    .subscribe-row { flex-direction: column; }
    .subscribe-inner { padding: 28px 20px; }
  }
  @media (max-width: 600px) {
    .hero, .article { padding: 0 16px; }
    .wide-wrap { padding: 0 16px; }
    h1 { font-size: 28px; }
    .story-box { padding: 18px 20px; }
  }
</style>
